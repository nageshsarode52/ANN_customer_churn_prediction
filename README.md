# Customer Churn Prediction

Streamlit app that predicts customer churn using a trained TensorFlow model.

## Run locally

```bash
uv sync
uv run streamlit run app.py
```

The app opens at `http://localhost:8501`.
